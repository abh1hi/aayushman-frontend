---
trigger: always_on
---

- **Semantic HTML:** Use correct HTML tags (`section`, `header`, `footer`, `nav`).
- **ARIA Usage:** Add ARIA attributes only where necessary (e.g., accordions, forms).
- **Keyboard Navigation:** Interactive elements must be keyboard accessible.
