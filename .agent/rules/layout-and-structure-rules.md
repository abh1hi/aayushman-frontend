---
trigger: always_on
---

- **Desktop-First:** Build desktop layout first, then adapt for smaller screens.
- **Grid & Flex Usage:** Use CSS Grid for section layouts and Flexbox for alignment.
- **Positioning:** Use absolute positioning only where required by the design (e.g., overlays, floating elements).
- **Section Isolation:** Each major UI section must live in its own Vue component.
